import { SearchBlock, SearchPanel } from "./searchBar.style";

const SearchBar = () => (
  <SearchBlock>
    <SearchPanel type="text" placeholder="Search" />
  </SearchBlock>
);

export default SearchBar;
